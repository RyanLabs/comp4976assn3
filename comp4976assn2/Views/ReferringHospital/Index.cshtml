@model IEnumerable<comp4976assn2.Models.SmartEntity.ReferringHospitalModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReferringHospital)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReferringHospital)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferringHospitalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferringHospitalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferringHospitalId })
        </td>
    </tr>
}

</table>
